import { PubPapersFilterPipe } from './pubPapers-filter.pipe';

describe('PubPapersFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PubPapersFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
