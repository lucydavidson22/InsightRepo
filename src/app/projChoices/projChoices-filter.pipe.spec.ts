import { ProjChoicesFilterPipe } from './projChoices-filter.pipe';

describe('ProjChoicesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjChoicesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
